(ns ^:no-doc xilk.xp.web-app.repl.internal.state
  "INTERNAL: data provider for stateful operations.")

(def module-ns-prefix "UNINITIALIZED")

(def app-module-kw :app)

(def default-theme-kw :default)
